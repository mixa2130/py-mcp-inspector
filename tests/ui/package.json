{
  "name": "pymcpinspector-ui-tests",
  "private": true,
  "type": "module",
  "description": "DOM tests for the inspector's sidebar. Run: npm install && node sidebar.test.mjs",
  "scripts": {
    "test": "node sidebar.test.mjs"
  },
  "dependencies": {
    "jsdom": "^26.0.0"
  }
}
